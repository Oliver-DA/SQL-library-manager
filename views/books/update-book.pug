extends ../layout 

block content 
    h1= title

    include book-errors

    form(action=`/books/${book.id}`, method = "POST")
        include form
        p
            input(type='submit' value='Update Book')
        
    include ../deleteForm