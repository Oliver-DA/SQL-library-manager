extends ../layout 

block content 
    h1= title

    include errors

    form(action ="/books/new", method = "POST")
        include form
        p
            input(type='submit' value='Create book')
        p
            a.button(href='/') Cancel